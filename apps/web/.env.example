# Optional: override the API host the web app talks to. Left unset, it
# defaults to http://<page-hostname>:3000 — which already works for both
# localhost and phone-over-LAN testing without editing anything.
# VITE_API_URL=http://localhost:3000
