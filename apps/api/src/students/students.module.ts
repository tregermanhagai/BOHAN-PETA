import { Module } from "@nestjs/common";

/**
 * Placeholder — student join/take-quiz flow (F-01–F-08a, PRD 3.1/3.8).
 * Next build slice: POST /assignments/join with the national_id lookup
 * fix (v2.5), then the paginated exam endpoints.
 */
@Module({})
export class StudentsModule {}
