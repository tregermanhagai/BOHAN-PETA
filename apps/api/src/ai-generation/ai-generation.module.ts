import { Module } from "@nestjs/common";

/**
 * Placeholder — Gemini-backed question generation (PRD section 6). Build
 * this behind a small provider interface (generateQuestions(),
 * improveQuestion(), reviseQuizFeedback()) per the PRD section 8 design
 * note, so swapping providers later is a contained change.
 */
@Module({})
export class AiGenerationModule {}
